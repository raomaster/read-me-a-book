# Backend core - FastAPI y dependencias web
fastapi
uvicorn
python-multipart
python-dotenv

# Audio processing - Necesario para todos los engines TTS
soundfile>=0.12.0
numpy>=2.0,<2.1
pydub

# TTS engines - Los tres motores soportados
gTTS                    # Google Text-to-Speech
coqui-tts>=0.26.2      # XTTS v2 y modelos Coqui
kokoro>=0.9.4          # Kokoro TTS para japonés y español
pyopenjtalk            # Requerido para voces japonesas de Kokoro
# PDF processing - Para extracción de texto de PDFs
pypdf
PyMuPDF                # fitz (para extracción de texto)
pdf2image              # Para convertir PDF a imágenes
pytesseract            # OCR para PDFs que son imágenes
Pillow                 # Requerido por pdf2image
# opencv-python          # Requerido por pytesseract
opencv-python==4.8.1.78  # Versión específica compatible

# EPUB processing - Para conversión y extracción de EPUBs
ebooklib               # Para leer EPUBs
beautifulsoup4         # Para parsear HTML en EPUBs

# Text processing - Utilidades de procesamiento de texto
anyascii>=0.3.0        # Para normalización de texto
pysbd>=0.3.4           # Para división de oraciones
num2words>=0.5.14      # Para conversión de números a palabras
f5-tts
